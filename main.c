// #include <stdio.h>
// #include <stdlib.h>
#include "External.h"
#include "Game.h"





int main(int argc, char *argv[]) {
    // srand(time( NULL));
    
    // free(bag);
    Setup(70,40);
    Play();
    getch();
    return 0;
}
